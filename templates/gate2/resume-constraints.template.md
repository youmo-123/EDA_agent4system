# Gate 2 恢复约束（示例模板）

- workflow_id：<workflow_id>
- workflow_round_id：<workflow_round_id>
- rtl_version_id：<rtl_version_id>
- 允许的下一步动作：见 `master-resolution.json` 的 `allowed_actions`
- 禁止的动作：见 `master-resolution.json` 的 `forbidden_actions`
- 必须提交的证据：见 `must_produce`
- 成功条件：见 `success_conditions`
- 重开条件：见 `reopen_conditions`
